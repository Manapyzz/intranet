{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
    <div class="row">

    <h1>Discipline : </h1>
    <table class="table table-sm">
        <tbody>
        {% for discipline in disciplineStudent %}
            <tr>
                <th>Name</th>
                <td>{{ discipline.name}}</td>
                <td><a href="{{ path('admin_discipline_delete',{'id':app.request.get('id'),'discipline_id': discipline.id}) }}">DELETE</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table><br>
{% endblock %}